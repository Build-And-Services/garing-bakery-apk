import 'package:flutter/material.dart';

class PrintTransactionPage extends StatelessWidget {
  const PrintTransactionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
